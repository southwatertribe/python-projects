Bouncing animation inspired by the DVD logo bouncer

The user can define the size of the window as well as the speed factor of the object that will be bouncing

The actual object can be changed within the code on line 39, just include the asset in the same directory